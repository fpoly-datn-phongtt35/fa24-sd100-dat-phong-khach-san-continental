@using Domain.Enums
@using ViewClient.Models.DTO
@using ViewClient.Views.Shared

@model RoomDetailViewModel

@{
    ViewData["Title"] = "Details";
    bool isUserLoggedIn = Context.Session.GetString("UserName") != null;
}
<div class="container">
    <input id="Id_Room" value="@Model.Room.Id" style="display:none"></input>
    <div class="mt-3">
        <h6 class="text-gold fw-bold">Contiental > Chi tiết phòng > @Html.DisplayFor(model => model.Room.Name)</h6>
    </div>
    <div class="p-5 rounded-2" style="background-color: rgb(239 239 239)">
        <div class="d-flex justify-content-end align-items-center mb-4">
            <a asp-action="Index" asp-controller="Home" class="btn btn-light">Quay lại</a>
        </div>

        <div class="row">
            <div class="col-md-7">
                <div class="luxury-carousel">
                    @if (Model.Room.Images != null && Model.Room.Images.Any())
                    {
                        <div id="roomImageCarousel" class="carousel slide" data-ride="carousel">
                            <div class="carousel-inner">
                                @for (int i = 0; i < Model.Room.Images.Count; i++)
                                {
                                    var img = Model.Room.Images[i];
                                    var imagePath = Url.Content($"~/images/{System.IO.Path.GetFileName(img.Image)}");

                                    <div class="carousel-item @(i == 0 ? "active" : "")">
                                        <img src="@imagePath" class="d-block w-100 luxury-img" alt="Room Image"
                                             onerror="this.onerror=null; this.src='/images/default.jpg';" />
                                    </div>
                                }

                            </div>
                            <button class="carousel-control-prev" type="button" data-bs-target="#roomImageCarousel" data-bs-slide="prev">
                                <span class="carousel-control-prev-icon"></span>
                            </button>
                            <button class="carousel-control-next" type="button" data-bs-target="#roomImageCarousel" data-bs-slide="next">
                                <span class="carousel-control-next-icon"></span>
                            </button>
                        </div>
                    }
                    else
                    {
                        <p>Không có hình ảnh</p>
                    }
                </div>
            </div>

            <div class="col-md-5">
                <div class="luxury-card p-4">
                    <h3 class="text-gold">Thông tin phòng</h3>
                    <p><strong>Loại phòng:</strong> @Html.DisplayFor(model => model.RoomType.Name)</p>
                    <p><strong>Diện tích:</strong> @Html.DisplayFor(model => model.Room.RoomSize) m²</p>
                    <p><strong>Sức chứa tối đa:</strong> @Html.DisplayFor(model => model.RoomType.MaximumOccupancy) người</p>
                    <p><strong>Giá:</strong> @Math.Round(Model.Room.Price ?? 0).ToString("N0") VNĐ/đêm</p>
                    <hr>
                    <button class="btn btn-gold mt-3" id="booking" data-bs-toggle="modal" data-bs-target="#bookingModal">Đặt phòng ngay</button>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="container pt-2">
    <div class="p-5 pb-1 pt-3 rounded-2" style="background-color: rgb(239 239 239);">
        <h4 class="text-gold fw-bold">Mô tả</h4>
        <p>@Html.DisplayFor(model => model.Room.Description)</p>
    </div>
</div>
<div class="container pt-2">
    <div class="p-5 pb-1 pt-3 rounded-2" style="background-color: rgb(239 239 239);">
        <h4 class="text-gold fw-bold">Tiện ích</h4>
        @if (Model.Room.RoomType.AmenityRooms != null && Model.Room.RoomType.AmenityRooms.Any())
        {
            <ul>
                @foreach (var amenityRoom in Model.Room.RoomType.AmenityRooms.Where(amenityRoom => amenityRoom.Status == EntityStatus.Active))
                {
                    <li><strong>@amenityRoom.Amenity.Name:</strong> @amenityRoom.Amount</li>
                }
            </ul>
        }
        else
        {
            <p>Phòng này không có tiện ích nào.</p>
        }
    </div>
</div>
<div class="container pt-2">
    <div class="p-5 pt-3 rounded-2" style="background-color: rgb(239 239 239)">
        <h4 class="text-gold fw-bold">
            <i class="fa-solid fa-pencil"></i>Phản hồi từ khách hàng
        </h4>

        @* noi dung *@
        <div class="container p-3 rounded-2" style="min-height:60px; background-color: #f8f9fa;">
            <!-- Input Comment -->
            @* <div class="d-flex align-items-center mb-3">
                <div class="rounded-circle bg-primary text-white d-flex justify-content-center align-items-center me-2" style="width:40px; height:40px; font-weight: bold;">?</div>
                <input type="text" class="form-control" id="messageInput" placeholder="Make Comments..." oninput="_feedback.toggleButton()" style="min-height: 70px;width:88%">
                <div class="btn ms-2 btn-gold" id="sendButton" style="display: none;width:8%" onclick="_feedback.SendComment()"><i class="fa-solid fa-paper-plane"></i>Gửi</div>
            </div> *@

            <!-- Comment Display -->
            <div id="ListCMT">
                
            </div>
        </div>
        @* noi dung *@


        @* paging *@
    </div>
</div>
<div class="modal fade" id="bookingModal" data-bs-keyboard="false" tabindex="-1" aria-labelledby="bookingModalLabel" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Thông Tin Đặt Phòng</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card-title h5">@Model.Room.Name</div>
                <p><i class="fa-regular fa-clock"></i> Giờ nhận phòng: <strong id="checkIn">Từ 14:00</strong></p>
                <p><i class="fa-regular fa-clock"></i> Giờ trả phòng: <strong id="checkOut">Trước 12:00</strong></p>
                <p>Giá: @Math.Round(Model.Room.Price ?? 0) VNĐ/đêm</p>

                @if (!isUserLoggedIn)
                {
                    <h6>Thông tin cá nhân:</h6>
                    <div class="row">
                        <div class="col-6">
                            <label for="firstName">Họ</label>
                            <input type="text" class="form-control" id="firstName" placeholder="Nhập họ">
                        </div>
                        <div class="col-6">
                            <label for="lastName">Tên</label>
                            <input type="text" class="form-control" id="lastName" placeholder="Nhập tên">
                        </div>
                    </div>
                    <div class="row mt-2">
                        <div class="col-6">
                            <label for="email">Email</label>
                            <input type="email" class="form-control" id="email" placeholder="Nhập email" required>
                        </div>
                        <div class="col-6">
                            <label for="phone">Số điện thoại</label>
                            <input type="text" class="form-control" id="phone" placeholder="Nhập số điện thoại" required>
                        </div>
                    </div>
                }

                <h6 class="mt-3">Dịch vụ thêm:</h6>
                <div class="service-list" style="max-height: 200px; overflow-y: auto;">
                    @if (Model.Services != null && Model.Services.Any())
                    {
                        foreach (var service in Model.Services)
                        {
                            <div class="form-check d-flex align-items-center mb-2">
                                <input class="form-check-input service-checkbox" type="checkbox" value="@service.Id" id="service_@service.Id">
                                <label class="form-check-label flex-grow-1" for="service_@service.Id">
                                    <strong>@service.Name</strong> - <span style="color: green;" data-price="@service.Price">
                                        @(Math.Round(@service.Price).ToString("N0")) VNĐ
                                    </span> / @(service.UnitName)
                                </label>
                                <input type="number" class="form-control service-quantity" id="quantity_@service.Id" min="1" value="1" style="width: 80px;">
                            </div>
                        }
                    }
                    else
                    {
                        <p>Không có dịch vụ nào hiện có.</p>
                    }
                </div>

                <hr />
                <div style="text-align: end;">
                    <p><strong>Đặt cọc:</strong> <span id="depositPayment">0 VNĐ</span></p>
                    <p><strong>Tiền phòng:</strong> <span id="totalRoomPayment">0 VNĐ</span></p>
                    <p><strong>Tiền dịch vụ:</strong> <span id="totalServicePayment">0 VNĐ</span></p>
                    <p><strong>Tổng tiền sau khi đặt cọc:</strong> <span id="totalPrice">0 VNĐ</span></p>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-primary" id="confirmBookingButton">Đặt phòng</button>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="bookingConfirmationModal" data-bs-keyboard="false" tabindex="-1"
     aria-labelledby="bookingConfirmationModalLabel" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="bookingConfirmationModalLabel">Xác nhận đặt phòng</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p><strong>Tên phòng:</strong> <h3 id="modalRoomName">Deluxe Room</h3></p>

                <div class="row mb-3">
                    <div class="col-6">
                        <label for="modalCheckIn" class="form-label"><strong>Ngày nhận phòng:</strong></label>
                    </div>
                    <div class="col-6">
                        <input type="text" id="modalCheckIn" name="checkIn" class="form-control" disabled>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-6">
                        <label for="modalCheckOut" class="form-label"><strong>Ngày trả phòng:</strong></label>
                    </div>
                    <div class="col-6">
                        <input type="text" id="modalCheckOut" name="checkOut" class="form-control" disabled>
                    </div>
                </div>

                <p><strong>Dịch vụ đã chọn:</strong> <span id="modalServices" class="text-muted">Không có dịch vụ nào</span></p>
                <p><strong>Tiền đặt cọc:</strong> <span id="modalDeposit" class="highlight-text">0 VNĐ</span></p>
                <p><strong>Tổng tiền:</strong> <span id="modalTotalPrice" class="highlight-text">0 VNĐ</span> (đã trừ tiền đặt cọc)</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-primary" id="confirmBooking">Xác nhận</button>
            </div>
        </div>
    </div>
</div>

<script>
    var isUserLoggedIn = @isUserLoggedIn.ToString().ToLower();
    var roomId = '@Model.Room.Id';
    var roomPrice = '@Model.Room.Price';
    var bookingUrl = '@Url.Action("RoomBooking", "RoomBooking")';
</script>
@section scripts {
    <script asp-append-version="true" src="~/module/roomBooking.js"></script>
    <script asp-append-version="true" src="~/module/feeback.js"></script>
}
<style>
    .modal-content {
        border-radius: 10px;
        border: none;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
    }


    .modal-footer {
        border-top: 1px solid #dee2e6;
    }

    .modal-body p {
        margin-bottom: 10px;
    }

    .modal-body h3 {
        font-size: 20px;
        font-weight: bold;
        margin-bottom: 15px;
    }

    .highlight-text {
        font-size: 18px;
        font-weight: bold;
        color: #d9534f;
    }

    .form-control[disabled] {
        background-color: #e9ecef;
        color: #495057;
        border: none;
        font-weight: bold;
        text-align: right;
    }


    .btn-secondary {
        background-color: #6c757d;
        border: none;
    }

        .btn-secondary:hover {
            background-color: #545b62;
        }
</style>